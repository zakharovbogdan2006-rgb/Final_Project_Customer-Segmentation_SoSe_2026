import pandas as pd
df = pd.read_csv(r"C:\Users\zakha\Downloads\salse.csv")
# df.to_excel("salse.xlsx", index= False)
import numpy as np
import math
import matplotlib.pyplot as plt
import seaborn as sns
from sklearn.cluster import KMeans
data = pd.read_csv(r"C:\Users\zakha\Downloads\salse.csv", header=0)
from sklearn.cluster import KMeans
from sklearn.preprocessing import StandardScaler
from sklearn.mixture import GaussianMixture
from scipy import stats
df["WEEK2_DAY08_DAY15_REVENUE_log"] = np.log1p(df["WEEK2_DAY08_DAY15_REVENUE"])
x = df["WEEK2_DAY08_DAY15_REVENUE_log"].dropna()
shape, loc, scale = stats.lognorm.fit(x)

plt.hist(x, bins=30, density=True, alpha=0.6)

xs = np.linspace(x.min(), x.max(), 1000)

plt.plot(
    xs,
    stats.lognorm.pdf(xs, shape, loc=loc, scale=scale),
    lw=2
)

plt.show()

